-VM 
--vm details con popup dell'immagine
--creazione vm non funziona se non c'è immagine e non si dice nulla o si fa nulla per comunicarlo allo user, nemmeno un required.

-- la vm non può essere aperta se non è nello stato open, ma anche qui non si fa nulla per comunicarlo allo user. Sembra semplicemente che non funzioni.


GENERALE
-Parallelizzare quante più richieste possibile

-Modificare gli header delle tabelle per renderle coerenti e comprensibili

-sistemare logica dei retry che ora prova le chiamate sempre tre volte.

-CONSEGNE ED ELABORATI:
--i respinti sono sempre visualizzati e non possono essere cancellati

--per quanto riguarda i compiti, nell'upload review dice che ritorna 400 ma in realtà carica la review nel momento in cui si carica sia versione che hw.

--lato server bisogna mettere un check per impedire che si possa caricare un assignment  con un timestamp già scaduto 



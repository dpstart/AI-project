- VM: la open per studenti non owner di una vM non funziona;

- VM: non c'è la funzione di share wonserhips per le VM; ogni owner di una vm dovrebbe poter condividere questa qualifica con qualsiasi altro membro del team;
